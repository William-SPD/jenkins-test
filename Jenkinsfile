pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Webhook triggered successfully'
                sh 'echo hello from jenkins'
            }
        }
    }
}
